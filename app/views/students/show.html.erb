
<p>
  <strong>Name:</strong>
  <%= @student.name %>
</p>

<p>
  <strong>Teacher:</strong>
  <%= @student.teacher.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @student.email %>
</p>

<%= link_to "Add another parent/guardian for #{@student.name}", new_parent_path(student_id: @student.id) %></br>
<%= link_to "Enter another grade for #{@student.name}", new_grade_path(student_id: @student.id) %></br>

<%= link_to 'Edit', edit_student_path(@student) %> |
<%= link_to 'Back', students_path %>
